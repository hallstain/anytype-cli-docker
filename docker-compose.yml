services:
  anytype-cli:
    build:
      context: .
      args:
        ANYTYPE_CLI_VERSION: v0.1.9
    ports:
      - "31112:31012"
      - "31110:31010"
      - "31111:31011"
    volumes:
      - ./anytype-cli-data:/data
    command: ["serve", "--listen-address", "0.0.0.0:31012"]
    restart: unless-stopped
